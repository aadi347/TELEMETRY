import AppRouter from "./routes/AppRouter";

/**
 * Root application component.
 * Renders the router which handles all page routing and layout.
 */
function App() {
  return <AppRouter />;
}

export default App;
